import React from 'react';
import {Article} from 'react-weui';

const Note = () => {
    return (
        <Article className={'fade-in'}>
            notes
        </Article>
    );
};

export default Note;
